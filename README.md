# Template de Site

Template simples em HTML/CSS/JS para começar um projeto.

Como visualizar:

- Abrir index.html no navegador.
- Ou rodar um servidor local (recomendado):

```bash
python -m http.server 8000
# depois abra http://localhost:8000
```

Personalize `styles.css` e `script.js` conforme necessário.